# Build intermediates — regenerated by `python3 src/build_cards.py`.
# The deliverables (out/print) and the review artefacts (out/proof) ARE committed.

# raw Chromium output, before the exact-artboard rewrap and logo stamping
out/.raw/

# generated HTML + the 1 MB base64 font bundle; edit src/concepts.py, not these
out/html/

# python bytecode
src/__pycache__/
